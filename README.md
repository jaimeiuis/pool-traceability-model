# pool-traceability-model
Some lab workflows require pooled testing with reflex retesting, but systems may not natively support parent/child traceability (pool ↔ constituent samples) and exception routing. This project demonstrates a minimal, queryable traceability model in SQLite.
